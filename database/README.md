# `database`

Database schemas, migrations, and data-model notes.

Authoritative design discussion lives in `docs/06-Database-Architecture.md`.

## Status

Scaffold only — **no database vendor (e.g. Supabase) configured yet**. Persistence decisions are deferred until architecture docs are authored.
